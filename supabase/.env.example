# Credenciais OAuth lidas pelo Supabase CLI ao subir o stack local.
# Copie para `supabase/.env` (gitignored) e preencha.

SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID=
SUPABASE_AUTH_EXTERNAL_GOOGLE_SECRET=
